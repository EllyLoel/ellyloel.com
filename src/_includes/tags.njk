{%- for tag in tags -%}
	{%- if tag !== exclude -%}
		<a href="/tags/{{ tag | slugify }}/" class="[ badge ] [ p-category ] [ neutral pill text-decoration-none ]">{{ tag }}</a>
	{%- endif -%}
{%- endfor -%}