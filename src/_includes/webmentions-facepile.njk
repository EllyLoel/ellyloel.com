<details class="[ facepile ]">
	<summary class="[ h3 ]">
		{{ facepileMentionsHeading }}
	</summary>
	<p>
		{%- for facepileMention in facepileMentions -%}
			{%- if loop.last and not loop.first %}
				and
			{% endif %}
			<a href="{{ facepileMention.author.url }}">
				{{- facepileMention.author.name | replace("?", "") | replace(r/:\w*:/g, "") | trim -}}
			</a>
			{%- if not loop.last and loop.length !== 2 -%}
				,
			{% endif %}
			{%- if loop.last -%}
				.
			{% endif %}
		{%- endfor -%}
	</p>
</details>
