import { styled } from "../../../../stitches.config";
import { Sub } from "@radix-ui/react-navigation-menu";

const NavMenuSub = styled(Sub, {
  display: "flex",
  flexDirection: "row",
  justifyContent: "space-between",
});

export default NavMenuSub;
