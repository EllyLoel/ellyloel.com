import { styled } from "../../../../stitches.config";
import { Item } from "@radix-ui/react-navigation-menu";

const NavMenuItem = styled(Item, {
  lineHeight: 1,
	padding: 0,
});

export default NavMenuItem;
